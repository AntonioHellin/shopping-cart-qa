# Lesson 29 start — Modelo A v2

This commit is the state **before** running the recorded prompt for lesson 29.

Construction rule: start from the original lesson result context, remove the prompt result, and preserve required preconditions/fixtures.
Source base snapshot: 6856d4a
Topic: performance percibida

## Lesson notes
- Para Exercise 1, extraé/wireá `AddToCartButton` en `ProductCard` o mantenelo standalone si seguís la prompt literal.
- Para Exercise 2, agregá `liked/likeCount` como parte del ejercicio; todavía no existe `LikeButton` ni optimistic UI.
